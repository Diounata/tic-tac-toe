import { TiArrowBackOutline } from 'react-icons/ti';
import { IconBaseProps } from 'react-icons/lib';

export default function BackIcon(props: IconBaseProps) {
    return <TiArrowBackOutline size={25} color='#e1e1e1' {...props} />;
}
