import { FaSortAmountDown } from 'react-icons/fa';
import { IconBaseProps } from 'react-icons';

export default function SortDown(props: IconBaseProps) {
    return <FaSortAmountDown {...props} />;
}
